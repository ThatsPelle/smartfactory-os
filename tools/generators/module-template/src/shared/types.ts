/**
 * Types shared between the module's server and UI.
 *
 * Keep this file small — most types should live in either server or ui.
 * Use this file only when a type genuinely crosses the boundary (typically:
 * API response shapes, common enums).
 */

export {};
