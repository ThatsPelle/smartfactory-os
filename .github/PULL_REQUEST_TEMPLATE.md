## Summary

What does this PR change, and why?

## Scope

- [ ] Touches a frozen architectural surface (envelope, manifest schema, permission naming) — ADR linked below.
- [ ] Adds a new package or module — ADR linked below.
- [ ] Adds new permissions, events, or capabilities (declared in manifest).
- [ ] Schema migration included (RLS verified).
- [ ] Documentation updated (MODULE.md / OWNERSHIP.md / ADR).

## Checks

- [ ] `pnpm lint` passes.
- [ ] `pnpm typecheck` passes.
- [ ] `pnpm validate:deps` passes (no boundary violations).
- [ ] `pnpm test` passes.
- [ ] No `any` introduced without comment.
- [ ] No string literals for permission keys or event types.

## ADRs

- (Link any ADR introduced or updated, or write "none".)

## Test plan

How was this verified? Include commands, screenshots, or test names.
